/** Fixture module for line-comment demos. */
export function greet(name: string): string {
  // TODO: demo inline review target
  return `hello, ${name}`;
}

export function add(a: number, b: number): number {
  return a + b;
}
